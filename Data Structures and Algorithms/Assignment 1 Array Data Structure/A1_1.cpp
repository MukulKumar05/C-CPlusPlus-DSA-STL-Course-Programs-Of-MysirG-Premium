#include<iostream>
using namespace std;

/*
A1.1 Define a class Array to implement array data structure with member
variables to store capacity of array, last index of the last filled block
of the array and a pointer to hold the address of the first block of the
dynamically created array.

(Completed)
*/
class Array
{
    private:
        int capacity, lastIndex;
        int *ptr;
};

int main()
{
    
    return 0;
}
